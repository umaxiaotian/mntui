# mntui
mntui is a lightweight terminal-based storage management tool for Linux. It simplifies common disk operations such as partitioning, formatting, mounting, and persistent mount configuration through an intuitive TUI, without requiring users to manually work with tools like `fdisk`, `mkfs`, `mount`, or `/etc/fstab`.
